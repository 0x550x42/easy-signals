{
  "scripts": {
    "deploy": "cd worker && npx wrangler deploy",
    "refresh": "source .env && curl -s -X POST https://signals-worker.ud0103.workers.dev/refresh -H \"x-refresh-token: $REFRESH_TOKEN\" | jq .",
    "deploy:refresh": "npm run deploy && npm run refresh"
  }
}
